name: Docker Release

on:
  push:
    tags:
      - "v*.*.*"
      - "v*.*.*-*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build (e.g., v1.2.3)"
        required: true
        type: string

permissions:
  contents: read
  packages: write

jobs:
  extract-metadata:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
      version: ${{ steps.meta.outputs.version }}
      is_prerelease: ${{ steps.meta.outputs.is_prerelease }}
      owner_lowercase: ${{ steps.meta.outputs.owner_lowercase }}
    steps:
      - name: Extract metadata
        id: meta
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ github.event.inputs.tag }}"
          else
            TAG=${GITHUB_REF#refs/tags/}
          fi
          VERSION=${TAG#v}
          echo "tag=${TAG}" >> $GITHUB_OUTPUT
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

          # Convert repository owner to lowercase for Docker tags
          OWNER_LOWER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "owner_lowercase=${OWNER_LOWER}" >> $GITHUB_OUTPUT

          # Check if this is a pre-release (alpha or beta)
          # Match versions like 1.2.3-alpha, 1.2.3-alpha.1, 1.2.3-beta, 1.2.3-beta.2
          if [[ "$VERSION" =~ -(alpha|beta)($|\.) ]]; then
            echo "is_prerelease=true" >> $GITHUB_OUTPUT
            echo "This is a pre-release version: $VERSION"
          else
            echo "is_prerelease=false" >> $GITHUB_OUTPUT
            echo "This is a stable release version: $VERSION"
          fi

          echo "Building Docker images for tag: ${TAG}"

  build-amd64:
    needs: extract-metadata
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push AMD64 image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile.server
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ needs.extract-metadata.outputs.owner_lowercase }}/mrrss:${{ needs.extract-metadata.outputs.version }}-amd64
            ${{ needs.extract-metadata.outputs.is_prerelease == 'false' && format('ghcr.io/{0}/mrrss:latest-amd64', needs.extract-metadata.outputs.owner_lowercase) || '' }}
          labels: |
            org.opencontainers.image.title=MrRSS
            org.opencontainers.image.description=A modern, privacy-focused RSS reader
            org.opencontainers.image.version=${{ needs.extract-metadata.outputs.version }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha,scope=amd64
          cache-to: type=gha,mode=max,scope=amd64

  build-arm64:
    needs: extract-metadata
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4
        with:
          platforms: linux/arm64

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ARM64 image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile.server
          platforms: linux/arm64
          push: true
          tags: |
            ghcr.io/${{ needs.extract-metadata.outputs.owner_lowercase }}/mrrss:${{ needs.extract-metadata.outputs.version }}-arm64
            ${{ needs.extract-metadata.outputs.is_prerelease == 'false' && format('ghcr.io/{0}/mrrss:latest-arm64', needs.extract-metadata.outputs.owner_lowercase) || '' }}
          labels: |
            org.opencontainers.image.title=MrRSS
            org.opencontainers.image.description=A modern, privacy-focused RSS reader
            org.opencontainers.image.version=${{ needs.extract-metadata.outputs.version }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha,scope=arm64
          cache-to: type=gha,mode=max,scope=arm64

  create-manifest:
    needs: [extract-metadata, build-amd64, build-arm64]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create and push multi-arch manifest
        run: |
          VERSION="${{ needs.extract-metadata.outputs.version }}"
          OWNER="${{ needs.extract-metadata.outputs.owner_lowercase }}"

          # Create version-specific manifest
          docker buildx imagetools create -t ghcr.io/${OWNER}/mrrss:${VERSION} \
            ghcr.io/${OWNER}/mrrss:${VERSION}-amd64 \
            ghcr.io/${OWNER}/mrrss:${VERSION}-arm64

          # Create latest manifest (only for stable releases)
          if [ "${{ needs.extract-metadata.outputs.is_prerelease }}" = "false" ]; then
            docker buildx imagetools create -t ghcr.io/${OWNER}/mrrss:latest \
              ghcr.io/${OWNER}/mrrss:latest-amd64 \
              ghcr.io/${OWNER}/mrrss:latest-arm64
          fi

      - name: Summary
        run: |
          OWNER="${{ needs.extract-metadata.outputs.owner_lowercase }}"
          VERSION="${{ needs.extract-metadata.outputs.version }}"

          echo "## Docker Release Completed! 🎉" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** ${{ needs.extract-metadata.outputs.tag }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Available Images" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Multi-architecture images:" >> $GITHUB_STEP_SUMMARY
          echo "- \`ghcr.io/${OWNER}/mrrss:${VERSION}\`" >> $GITHUB_STEP_SUMMARY

          if [ "${{ needs.extract-metadata.outputs.is_prerelease }}" = "false" ]; then
            echo "- \`ghcr.io/${OWNER}/mrrss:latest\`" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Architecture-specific images:" >> $GITHUB_STEP_SUMMARY
          echo "- \`ghcr.io/${OWNER}/mrrss:${VERSION}-amd64\`" >> $GITHUB_STEP_SUMMARY
          echo "- \`ghcr.io/${OWNER}/mrrss:${VERSION}-arm64\`" >> $GITHUB_STEP_SUMMARY

          if [ "${{ needs.extract-metadata.outputs.is_prerelease }}" = "false" ]; then
            echo "- \`ghcr.io/${OWNER}/mrrss:latest-amd64\`" >> $GITHUB_STEP_SUMMARY
            echo "- \`ghcr.io/${OWNER}/mrrss:latest-arm64\`" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Pull Command" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "docker pull ghcr.io/${OWNER}/mrrss:${VERSION}" >> $GITHUB_STEP_SUMMARY

          if [ "${{ needs.extract-metadata.outputs.is_prerelease }}" = "false" ]; then
            echo "# or" >> $GITHUB_STEP_SUMMARY
            echo "docker pull ghcr.io/${OWNER}/mrrss:latest" >> $GITHUB_STEP_SUMMARY
          fi

          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
